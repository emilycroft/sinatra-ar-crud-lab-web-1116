<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @posts.each do |post|%>
      <%= post.name %>
      <%= post.content %>
    <% end%>
  </body>
</html>


<!--That view should use erb to iterate over @posts and render them on the page.-->
